// Fractal
    // Fraclet
    @Component two variables name and the interface they provide
    @Interface name and signature
    @Requires name role signature  contingency cardinality
        Example:
        // Serveur
        @Component(name="s" provides=@Interface(name="s" signature=Service.class))

        // Client
        @Component(name="r" provides=@Interface(name="r" signature=Runnable.java))
        @Requires(name="s")

    // Fractal ADL



    // Fractal API
